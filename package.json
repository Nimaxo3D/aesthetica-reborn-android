{
  "name": "aesthetica-reborn-android",
  "version": "1.0.0",
  "private": true,
  "description": "Aesthetica Reborn Android APK project. No npm install required for GitHub Actions build.",
  "scripts": {
    "build:android": "cd android && ./gradlew assembleDebug"
  }
}
